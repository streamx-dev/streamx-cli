eventType: page_publish
eventSource: source

relativePathLevel: 1
key: ${relativePath}
ignorePatterns:
  - ".*/ignore\\.me"

payload:
  content:
    bytes: file://${payloadPath}
  type: page/sub-page

